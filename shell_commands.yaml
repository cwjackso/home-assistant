### SHELL COMMANDS ###

generate_garden_gif: 'convert -delay 100 -loop 0 /shared/files/garden_camera/*.jpg /shared/files/garden_camera/garden.gif'

generate_front_door_gif: 'convert -delay 100 -loop 0 /shared/files/front_door_camera/*.jpg /shared/files/front_door_camera/front.gif'
